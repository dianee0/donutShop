# Local SQLite (development only)
# .env example perhaps for later...
DATABASE_URL="file:./dev.db"

# Example Postgres connection (production/staging)
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

# Optional: Email service API key (Mailchimp, Postmark, etc.)
# EMAIL_API_KEY="your-api-key-here"

# Optional: App secrets
# NEXTAUTH_SECRET="example-secret"
# NEXT_PUBLIC_API_URL="http://localhost:3000"